/*int	board_check(int board[], int col, int row)
{
	int	i;

	i = 0;
	while (i < col)
	{
		if (board[i] == row)
			return (0);
		if (col - i == row - board[i] || col - i == - row + board[i])
			return (0);
		i += 1;
	}
	return (1);
}

void	next_queen(board[], int col, int *total_count)
{
	if (col == 10)
	//print
	//newline
	// totalcoount+1
	// backtrack
	if (board_check(*board, col, row))

}
	int	ft_ten_queens_puzzle(void)
{
	int	board[10];
	int	total_count;

	total_count = 0
}

int	main(void)
{
	ft_ten_queens_puzzle
}
